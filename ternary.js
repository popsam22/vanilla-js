//A shoter version of finding if a num is even or odd using ternary op


let num = 17;
let result;

// ternary operator = ?: best used in a two fork way 

result = num%2===0 ? "Even" : "Odd";
console.log(result);